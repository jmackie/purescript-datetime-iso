{
    "private": true,
    "scripts": {
        "clean": "rimraf output && rimraf .pulp-cache",
        "build": "pulp build -- --censor-lib --strict",
        "test": "pulp test"
    },
    "dependencies": {
        "pulp": "^12.0.3",
        "purescript": "^0.12",
        "rimraf": "^2.6.2"
    }
}
