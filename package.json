{
    "private": true,
    "scripts": {
        "clean": "rimraf output && rimraf .pulp-cache",
        "build": "spago build",
        "test": "spago test"
    },
    "dependencies": {
        "purescript": "^0.13",
        "rimraf": "^2.6.2"
    }
}
